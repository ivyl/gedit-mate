Gedit set of plugins, color shemes, etc.

Installation:

rake install:local
sudo rake install:global

From now on you can use "g 'file'" command which doesn't bother terminal.
Enable plugins in gedit preferences, choose font (monaco, consolas or DejaVu Sans Mono are great) and theme.

Have fun!

Based on GMate by lexrupy.
